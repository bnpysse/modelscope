# 天衍五维 · 全息量化作战系统架构准则与运行规范 (TianYan System Rules)

## 一、 系统核心物理架构与三节点协同体系

1. **N100 边缘量化数据中心 (Edge Center)**
   - **硬件与角色**: 本地 7x24 小时低功耗节点，分配 384GB 存储空间（可用 >300GB）。
   - **核心职责**: 全市场 5,555 只 A 股 Level 2 资金流向与微观筹码基础特征的高速收关采集、FIFO 滑动窗口维护、DuckDB 持久化。
   - **专库专用**: `tianyan_l2_66d.duckdb` / `tianyan_l2_132d.duckdb`。

2. **阿里云 DSW 生产计算节点 (DSW Cloud Station)**
   - **硬件与角色**: 阿里云弹性 CPU 实例（28GB RAM，无 GPU）。
   - **核心职责**: DuckDB 内存列式高速检索、Streamlit 前端 (`quant.donglida.com`) 交互总台、偏微分方程（MCD 连续场）毫秒级初筛。
   - **内存分配策略**: 28GB 内存 100% 留给 DuckDB 数据计算与前端并发，严禁在本地强行加载大模型。

3. **ModelScope 云端千卡 GPU 集群 (AI Cognitive Core)**
   - **角色**: 阿里云千卡 GPU 算力集群常驻模型（32B / 235B / MiniMax M1）。
   - **核心职责**: 接收客观物理截面真值，执行第二阶段思维链（CoT）单兵战术推演与 4 级动态仓位裁决。
   - **协同优势**: 零本地内存占用、开机免等待加载、永久免费。

---

## 二、 统帅战术双阶段决断铁律

1. **第一阶段：微分方程硬核初筛 (确定性物理过滤)**
   - 全市场股票首先通过微积分与流体力学算法（CPR 锁仓刚性、BRI 断层真空、κCYC 斐波收敛、ΔCYS 盈亏剪刀差、SMPI 主力筹码纯度）解算。
   - 纯数学确定性输出，保证 0 幻觉、0 滞后。

2. **第二阶段：32B 大模型单兵战术深度推演**
   - **Prompt 完整性铁律**: 严禁在向大模型组织提示词时遗漏参数！
   - 必须通过 `build_physics_context_block` 完整注入：
     - 行情参数：Close, ΔP% (Pct_Change), Turnover%
     - 筹码参数：LFS, HCCYF13, ASR, Z, X70, X90, Y_Overlap
     - 成本与盈亏：CYC5/13/34/inf, CYS13, CYS34, BIAS_5_20, Norm_BIAS
     - 资金分布：Main%, Dare%, D_pos, CYF66_Raw, VMA55, ΔCYF
     - 预解算六大高阶衍生张量真值：CPR, ηV, BRI, κCYC, ΔCYS, SMPI
   - 严格杜绝大模型研报出现“未提供某指标 -> 无法计算”的现象。

---

## 三、 自选观察池与战备库治理规范

1. **库表解耦隔离**:
   - 统帅个人自选与战备池由专库 `tianyan_watchlist.duckdb` 和 `battle_plan.json` 双模持久化，严禁污染全量市场特征底座。
2. **三维度管理功能**:
   - `➕ 添加标的到分组`：单标的精准入池；
   - `📁 新建自选分组`：自定义创建战备专题池；
   - `✏️ 维护/修改分组标的`：支持双击即时重命名标的、单选/多选批量移出、一键清空分组、注销自定义分组。
3. **战备雷达榜交互标准**:
   - 默认激活【🎯 仅选前三 (Top 3)】，提供【⚪ 全不选】与【🔘 全选】极速辅助器；
   - 核心先锋标的胶囊按钮必须配置 Hover Tooltip，清晰展示计算公式、代入参数与物理意义。

---

## 四、 存储容量估算与 132 交易日滚动计算科学准则

1. **存储容量量化核算**:
   - 全市场 5,555 只 A 股：
     - 66 交易日日频特征截面 (DuckDB 列式压缩)：约 **30MB ~ 50MB**；
     - 132 交易日日频特征截面 (DuckDB 列式压缩)：约 **60MB ~ 100MB**；
     - 5 分钟频筹码大单分布 (132 交易日)：约 **2.5GB ~ 5GB**。
   - **结论**: N100 当前分配的 384GB（可用 >320GB）极其充沛，完全无需扩容，剩余空间可自由用于搭建其他虚拟机。

2. **滚动窗口与增量计算法则 ($O(1)$ 复杂度)**:
   - **FIFO 滑动窗口机制**:
     每日收盘（15:00~16:00）后，新增当日第 $T$ 日数据，并执行淘汰：
     `DELETE FROM l2_table WHERE date < T_132_DAYS_AGO;`
   - **严禁 132 天全量重新计算**:
     - **筹码分布**: 满足马尔可夫状态转移一阶递推公式：
       $$D_t(P) = D_{t-1}(P) \times (1 - H_t) + \Delta V_t(P)$$
       仅需昨日分布与今日换手率，一步递推完成；
     - **累积量能与均线**: 满足滑动窗口公式：
       $$\text{Sum}_{132}(T) = \text{Sum}_{132}(T-1) + \text{Net}(T) - \text{Net}(T-132)$$
       一步加减完成；
     - **高阶张量**: 仅依赖最新截面状态与近 3~5 日差分。
   - 全市场 5,555 只股票增量更新在 N100 上几分钟内闭环，计算高效平稳。
